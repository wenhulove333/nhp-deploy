{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "docker-compose build (no cache)",
        "type": "shell",
        "command": "docker compose build --no-cache",
        "problemMatcher": [],
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }
